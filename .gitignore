# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
.eggs/
*.egg
.Python
env/
venv/
.venv/

# ML artifacts (large binary model files)
*.pkl
*.joblib
# EXCEPCIÓN: los modelos del ensemble ACTIVO sí se versionan (12MB, sklearn).
# Permiten que quien clone vea el monitor funcionando sin pasar archivos por fuera.
!services/ml-service/ml_artifacts_ensemble_v1/*.pkl
# ML artifact backups
ml_artifacts_backup_*/

# Node
node_modules/
.next/
out/
.turbo/

# Environment
.env
.env.local
.env.*.local

# Claude Code
.claude/
CLAUDE.md

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Testing
.coverage
htmlcov/
.pytest_cache/
coverage/

# Docker
*.log

# OS
Thumbs.db

# Files
datasets/
dataset/
logs/
.claude

# Documentacion tecnica del proyecto (specs, backlog, reglas) SI se versiona.
# Se excluyen entregables binarios y material no-repo.
reports/*
mockups/*
*.xlsx
*.ppt
*.pptx
*.docx
*.txt
frontend/playwright-report/
frontend/test-results/
catalogos_amvu.json
ml.md
run_tests.sh
*.png
*.csv

# Datasets y experimentos ML pesados (no van al repo)
services/ml-proposal/
services/ml-service/data/
services/ml-service/ml_artifacts_v2/
services/ml-service/ml_artifacts_v2_1/
# Experimentos/backends ML no activos (PRs separados)
services/ml-service-isolation/
services/ml-service/scripts/v2/
services/ml-service/scripts/v2_1/
services/ml-service/scripts/health_monitor_poc/

# --- Terraform / Despliegue AWS (NUNCA commitear estado ni secretos) ---
# Estado de Terraform (puede contener secretos en texto plano)
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl
crash.log
crash.*.log
# Variables con secretos (db_password, secret_key, credenciales SMTP)
*.tfvars
*.tfvars.json
!*.tfvars.example
# Entorno de producción generado en la EC2 / local
.env.prod
secrets.local*
# Bundle de código subido a S3 para el deploy (repo privado)
airmon-app-bundle.tar.gz
